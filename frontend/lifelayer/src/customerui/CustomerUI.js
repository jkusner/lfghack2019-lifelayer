import React from 'react'
import { Button, Table, Select, Input, Modal } from 'semantic-ui-react'

export default class CustomerUI extends React.Component {
  state = {modalOpen: true}
  render() {
    return (
      "CustomerUI!"
    );
  }
}